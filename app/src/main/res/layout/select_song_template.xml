<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="70dp"
    android:background="@color/white">
    <ImageView
        android:src="@drawable/initial_photo"
        android:id="@+id/album_cover_photo"
        android:layout_toRightOf="@+id/selected"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginLeft="10dp"
        android:layout_centerVertical="true"/>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:layout_toRightOf="@+id/album_cover_photo"
        android:orientation="vertical"
        android:gravity="center_vertical">
        <TextView
            android:textSize="14dp"
            android:id="@+id/title"
            android:text="title"
            style="@style/Music.PlaylistText"/>
        <TextView
            android:textSize="14dp"
            android:id="@+id/artist"
            android:text="artist"
            style="@style/Music.PlaylistText"/>
        <TextView
            android:textSize="14dp"
            android:id="@+id/duration"
            android:text="00:00:00"
            style="@style/Music.PlaylistText"/>
    </LinearLayout>
    <TextView
        android:id="@+id/selected"
        android:layout_centerVertical="true"
        android:layout_marginLeft="10dp"
        android:background="@drawable/playlist_selection_radio_button"
        android:layout_width="30dp"
        android:layout_height="30dp" />
</RelativeLayout>